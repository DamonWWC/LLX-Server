# 林龍香大米商城 - 环境变量示例
# 使用方法: 复制此文件为 .env 并修改密码
# cp env.example .env

# ==================== 数据库配置 ====================

# PostgreSQL 数据库密码
# 要求: 至少 16 位，包含大小写字母、数字、特殊字符
DB_PASSWORD=your_strong_db_password_change_me

# ==================== 缓存配置 ====================

# Redis 密码
# 要求: 至少 16 位，包含大小写字母、数字、特殊字符
REDIS_PASSWORD=your_strong_redis_password_change_me

# ==================== 应用配置 ====================

# 运行环境: Development, Staging, Production
ASPNETCORE_ENVIRONMENT=Production

# ==================== 安全提示 ====================

# ⚠️ 重要: 请务必修改上述默认密码
# ⚠️ 不要将包含真实密码的 .env 文件提交到 Git
# ⚠️ 生产环境建议使用密钥管理服务（如 Azure Key Vault）

# ==================== 可选配置 ====================

# 自定义端口映射（如果需要）
# API_PORT=8080
# DB_PORT=5432
# REDIS_PORT=6379

# 数据库连接池配置
# DB_MIN_POOL_SIZE=5
# DB_MAX_POOL_SIZE=100

# Redis 缓存配置
# REDIS_MAX_MEMORY=256mb
# REDIS_EVICTION_POLICY=allkeys-lru

